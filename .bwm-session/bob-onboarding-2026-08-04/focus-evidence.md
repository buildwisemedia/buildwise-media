# Focus evidence

Every editable field has an explicit label. The verification code receives focus only after a successful start response. Buttons use native controls, status copy uses an `aria-live` region, and the hidden invitation state exposes no actionable form.
