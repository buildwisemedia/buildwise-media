# Desktop evidence

The built consent page was inspected at 1280 pixels wide. The document width equaled the viewport width. A valid opaque invitation displayed the details form while the verification and success sections had zero rendered height. Without a token, the warning displayed and the form computed to `display:none`.
