# SEO and technical evidence

The private invitation surface emits `noindex,nofollow`, a canonical URL, `no-referrer`, and no tracking globals. The invitation token is read only from the query string and is never rendered into visible copy.
