# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# legacy scrape artifacts (pre-Black-Gold reference)
site-download/

# wrangler local cache
.wrangler/

# bwm-website-builder skill session markers (transient)
.bwm-preflight-passed

# draft staging for /playbook ship pipeline (Sonnet → Opus QA → Codex)
.tmp/

# print business card design iterations & mockups (binaries stored on R2)
public/assets/print/business-card/iterations/

# brand visual-QA bundles (scripts/brand-closure-visual-qa.mjs): heavy per-viewport
# screenshots + per-route raw data, regenerated every `npm run qa:brand:visual` (~220MB/run).
# Reviewable summaries stay tracked (visual-qa-summary.*, manual-visual-review.md, *-audit.*,
# brand-closure-qa.*). Durable full-image proof belongs in R2 (bwm-evidence-bundles/), not git.
_verification/**/full-page.png
_verification/**/probe.png
_verification/**/metrics.json
_verification/**/browser-events.json
