---
// Fit diagnostic gating the booking embed on /book.
// On submit: POSTs to bwm-form-handler /buildwisemedia/discovery-qualifier.
// On qualified: reveals a Revenue Leak Map summary, then the booking calendar.
// On disqualified: redirects to /thank-you-resource?reason=<reason>.
const poorFour = [
  { value: 'invisible_market', label: 'Invisible Market', body: 'When buyers search, ask AI, or compare, you are not the name that comes up first.' },
  { value: 'leaky_revenue', label: 'Leaky Revenue', body: 'Leads, calls, quotes, and past customers slip away before they turn into booked work.' },
  { value: 'owner_bottleneck', label: 'Owner Bottleneck', body: 'Growth still runs through you — stop watching the marketing for a week and it stalls.' },
  { value: 'reactive_growth', label: 'Reactive Growth', body: 'The numbers reach you late, so decisions feel rushed and every fix is a scramble.' },
];
---

<form id="qualifier-form" class="qualifier-form" novalidate data-current-step="1">
  <div class="q-progress" role="progressbar" aria-label="Diagnostic progress" aria-valuemin="1" aria-valuemax="5" aria-valuenow="1">
    <span class="q-progress-bar" data-progress-bar></span>
  </div>

  <header class="q-head">
    <p class="q-eyebrow mono">REVENUE LEAK MAP · FIVE MINUTES</p>
    <h2 class="q-heading">Build your Revenue Leak Map.</h2>
    <p class="q-sub">The goal is a useful first map: where your revenue is leaking now, which Ascend layer to move first, and whether the numbers justify a real conversation.</p>
    <button type="button" class="q-submit q-mobile-start" data-mobile-next>Continue</button>
  </header>

  <div class="q-layout">
    <div class="q-workspace">
      <section class="q-step is-active" data-step="1" aria-labelledby="q-step-1">
        <p class="q-step-kicker mono">Step 1 / 5</p>
        <h3 id="q-step-1" class="q-step-title">Who are we mapping?</h3>
        <div class="q-grid">
          <div class="q-row">
            <label class="q-label" for="q-first-name">First name</label>
            <input id="q-first-name" name="first_name" type="text" required autocomplete="given-name" />
          </div>
          <div class="q-row">
            <label class="q-label" for="q-company">Company</label>
            <input id="q-company" name="company" type="text" required autocomplete="organization" />
          </div>
          <div class="q-row">
            <label class="q-label" for="q-trade">Trade / category</label>
            <input id="q-trade" name="trade" type="text" required placeholder="HVAC, roofing..." />
          </div>
          <div class="q-row q-row--wide">
            <label class="q-label" for="q-service-area">Primary service area</label>
            <input id="q-service-area" name="service_area" type="text" required placeholder="Atlanta, Tampa, Greenville..." />
          </div>
        </div>
      </section>

      <section class="q-step" data-step="2" aria-labelledby="q-step-2">
        <p class="q-step-kicker mono">Step 2 / 5</p>
        <h3 id="q-step-2" class="q-step-title">Which market leaks feel active?</h3>
        <div class="q-options q-options--grid">
          {poorFour.map((item) => (
            <label class="q-option-card">
              <input type="checkbox" name="selected_poor_four" value={item.value} />
              <span>
                <strong>{item.label}</strong>
                <em>{item.body}</em>
              </span>
            </label>
          ))}
        </div>
      </section>

      <section class="q-step" data-step="3" aria-labelledby="q-step-3">
        <p class="q-step-kicker mono">Step 3 / 5</p>
        <h3 id="q-step-3" class="q-step-title">Where is revenue leaking now?</h3>
        <fieldset class="q-fieldset">
          <legend class="q-label">Lead flow right now</legend>
          <div class="q-options">
            <label><input type="radio" name="lead_flow_state" value="too_few" required /><span>Too few qualified leads</span></label>
            <label><input type="radio" name="lead_flow_state" value="inconsistent" /><span>Good months, quiet months</span></label>
            <label><input type="radio" name="lead_flow_state" value="enough_but_messy" /><span>Enough leads, messy follow-through</span></label>
          </div>
        </fieldset>
        <fieldset class="q-fieldset">
          <legend class="q-label">Follow-up system</legend>
          <div class="q-options">
            <label><input type="radio" name="follow_up_state" value="manual" required /><span>Mostly manual</span></label>
            <label><input type="radio" name="follow_up_state" value="partial" /><span>Some automation, not trusted</span></label>
            <label><input type="radio" name="follow_up_state" value="instrumented" /><span>Instrumented and visible</span></label>
          </div>
        </fieldset>
      </section>

      <section class="q-step" data-step="4" aria-labelledby="q-step-4">
        <p class="q-step-kicker mono">Step 4 / 5</p>
        <h3 id="q-step-4" class="q-step-title">What have you already tried?</h3>
        <fieldset class="q-fieldset">
          <legend class="q-label">Agency history</legend>
          <div class="q-options">
            <label><input type="radio" name="agency_history" value="burned_before" required /><span>Burned by one or more agencies</span></label>
            <label><input type="radio" name="agency_history" value="mixed_results" /><span>Some wins, not a system</span></label>
            <label><input type="radio" name="agency_history" value="mostly_internal" /><span>Mostly handled internally</span></label>
          </div>
        </fieldset>
        <fieldset class="q-fieldset">
          <legend class="q-label">AI comfort</legend>
          <div class="q-options">
            <label><input type="radio" name="ai_comfort" value="ready" required /><span>Ready — as long as a person signs off before anything goes live</span></label>
            <label><input type="radio" name="ai_comfort" value="curious_cautious" /><span>Curious, but I want to see it work before I trust it</span></label>
            <label><input type="radio" name="ai_comfort" value="skeptical" /><span>Been burned before — I'll believe the numbers, not the pitch</span></label>
          </div>
        </fieldset>
      </section>

      <section class="q-step" data-step="5" aria-labelledby="q-step-5">
        <p class="q-step-kicker mono">Step 5 / 5</p>
        <h3 id="q-step-5" class="q-step-title">Can the system get enough fuel to work?</h3>
        <div class="q-grid">
          <div class="q-row">
            <label class="q-label" for="q-revenue">Operating scale</label>
            <select id="q-revenue" name="revenue_range" required>
              <option value="">Select one</option>
              <option value="under_1m">Early-stage operation</option>
              <option value="1m_3m">Growing local operator</option>
              <option value="3m_8m">Established service team</option>
              <option value="8m_15m">Multi-location or expansion</option>
              <option value="15m_plus">Market leader</option>
            </select>
          </div>
          <div class="q-row">
            <label class="q-label" for="q-offer">Best-fit entry point</label>
            <select id="q-offer" name="offer_interest" required>
              <option value="">Select one</option>
              <option value="ascend_pilot">Ascend Pilot</option>
              <option value="ascend">Ascend</option>
              <option value="ascend_pro">Ascend Pro</option>
              <option value="not_sure">Not sure yet</option>
            </select>
          </div>
        </div>
        <fieldset class="q-fieldset">
          <legend class="q-label">Ad-spend readiness</legend>
          <div class="q-options">
            <label><input type="radio" name="ad_spend_readiness" value="ready_1to1" required /><span>Ready for the 1:1 ad-spend match if the plan is strong</span></label>
            <label><input type="radio" name="ad_spend_readiness" value="need_math" /><span>Need to see the math first</span></label>
            <label><input type="radio" name="ad_spend_readiness" value="not_ready" /><span>Not ready for meaningful distribution budget</span></label>
          </div>
        </fieldset>
        <fieldset class="q-fieldset">
          <legend class="q-label">Are you the decision-maker on marketing investments?</legend>
          <div class="q-options">
            <label><input type="radio" name="decision_maker" value="yes_i_decide" required /><span>Yes, I make the call</span></label>
            <label><input type="radio" name="decision_maker" value="i_influence" /><span>I influence the decision</span></label>
            <label><input type="radio" name="decision_maker" value="no_someone_else" /><span>No, someone else decides</span></label>
          </div>
        </fieldset>
        <div class="q-row q-row--wide q-email-final">
          <label class="q-label" for="q-email">Where should we send your Revenue Leak Map?</label>
          <input id="q-email" name="email" type="email" required autocomplete="email" inputmode="email" placeholder="you@company.com" />
        </div>
      </section>

      <div class="q-actions">
        <button type="button" class="q-secondary" data-prev hidden>Back</button>
        <button type="button" class="q-submit" data-next>Continue</button>
        <button type="submit" class="q-submit" id="q-submit-btn" hidden>
          <span class="q-submit-label">Build My Revenue Leak Map</span>
          <span class="q-submit-loading" hidden>Building...</span>
        </button>
      </div>

      <p class="q-error" id="q-error" hidden></p>
    </div>

    <aside class="q-map-rail" aria-label="Revenue Leak Map building">
      <div class="q-map-top">
        <p class="q-eyebrow mono">REVENUE LEAK MAP · BUILDING</p>
        <strong data-rail-status>Identity layer open</strong>
      </div>
      <div class="q-map-meter" aria-hidden="true">
        <span data-rail-meter></span>
      </div>
      <dl class="q-map-signals">
        <div>
          <dt class="mono">Business</dt>
          <dd data-rail-business>Waiting</dd>
        </div>
        <div>
          <dt class="mono">Active leaks</dt>
          <dd data-rail-leaks>None selected yet</dd>
        </div>
        <div>
          <dt class="mono">Revenue state</dt>
          <dd data-rail-revenue>Pending</dd>
        </div>
        <div>
          <dt class="mono">Readiness</dt>
          <dd data-rail-readiness>Pending</dd>
        </div>
      </dl>
      <div class="q-map-routing">
        <span class="dot cyan" aria-hidden="true"></span>
        <p>The call gets sharper as this fills in. If we're not a fit, you'll know before you book a slot.</p>
      </div>
    </aside>
  </div>
</form>

<section id="market-leak-map" class="market-leak-map" hidden aria-live="polite">
  <p class="q-eyebrow mono">REVENUE LEAK MAP · PREVIEW</p>
  <h2 class="q-heading">Here's what we'll dig into on your call.</h2>
  <div class="map-grid">
    <div>
      <p class="map-label mono">Primary leaks</p>
      <p class="map-value" data-map-leaks>Pending</p>
    </div>
    <div>
      <p class="map-label mono">Revenue state</p>
      <p class="map-value" data-map-revenue>Pending</p>
    </div>
    <div>
      <p class="map-label mono">Readiness</p>
      <p class="map-value" data-map-readiness>Pending</p>
    </div>
  </div>
  <p class="map-note">If we're a fit, we'll use this preview on the call to decide whether Ascend Pilot, Ascend, or Ascend Pro is right for you.</p>
</section>

<script is:inline>
(function () {
  const form = document.getElementById('qualifier-form');
  const submitBtn = document.getElementById('q-submit-btn');
  const nextBtn = form?.querySelector('[data-next]');
  const mobileNextBtn = form?.querySelector('[data-mobile-next]');
  const prevBtn = form?.querySelector('[data-prev]');
  const errorEl = document.getElementById('q-error');
  const steps = form ? Array.from(form.querySelectorAll('[data-step]')) : [];
  const progressWrap = form?.querySelector('.q-progress');
  const progress = form?.querySelector('[data-progress-bar]');
  const railStatus = form?.querySelector('[data-rail-status]');
  const railMeter = form?.querySelector('[data-rail-meter]');
  const railBusiness = form?.querySelector('[data-rail-business]');
  const railLeaks = form?.querySelector('[data-rail-leaks]');
  const railRevenue = form?.querySelector('[data-rail-revenue]');
  const railReadiness = form?.querySelector('[data-rail-readiness]');
  let currentStep = 1;

  if (!form || !submitBtn || !nextBtn || !prevBtn || !errorEl) return;

  function event(name, params, opts) {
    try {
      let payload = Object.assign({}, params || {});
      if (typeof window.__bwmGetAttributionPayload === 'function') {
        payload = Object.assign(window.__bwmGetAttributionPayload(), payload);
      }
      if (typeof window.__bwmTrackEvent === 'function') {
        window.__bwmTrackEvent(name, payload, opts || {});
        return;
      }
      window.dataLayer = window.dataLayer || [];
      window.dataLayer.push(Object.assign({ event: name }, payload));
      window.gtag?.('event', name, Object.assign({ transport_type: 'beacon' }, payload));
    } catch (_) {}
  }

  function showStep(step) {
    currentStep = Math.max(1, Math.min(step, steps.length));
    steps.forEach((el) => el.classList.toggle('is-active', Number(el.getAttribute('data-step')) === currentStep));
    form.setAttribute('data-current-step', String(currentStep));
    if (progressWrap) progressWrap.setAttribute('aria-valuenow', String(currentStep));
    if (progress) progress.style.transform = `scaleX(${currentStep / steps.length})`;
    prevBtn.hidden = currentStep === 1;
    nextBtn.hidden = currentStep === steps.length;
    if (mobileNextBtn) mobileNextBtn.hidden = currentStep !== 1;
    submitBtn.hidden = currentStep !== steps.length;
    updateMapRail();
    event('diagnostic_step_complete', { diagnostic_step: currentStep });
  }

  function activeFields() {
    const active = form.querySelector(`[data-step="${currentStep}"]`);
    return active ? Array.from(active.querySelectorAll('input, select, textarea')) : [];
  }

  function validateStep() {
    const fields = activeFields();
    const groups = new Map();
    for (const field of fields) {
      if (field.type === 'checkbox') continue;
      if ((field.type === 'radio') && field.required) {
        groups.set(field.name, fields.filter((f) => f.name === field.name));
        continue;
      }
      if (!field.checkValidity()) {
        field.reportValidity();
        return false;
      }
    }
    for (const group of groups.values()) {
      if (!group.some((field) => field.checked)) {
        group[0].reportValidity();
        return false;
      }
    }
    return true;
  }

  function attributionPayload() {
    if (typeof window.__bwmGetAttributionPayload === 'function') {
      return window.__bwmGetAttributionPayload();
    }
    let stored = {};
    try { stored = JSON.parse(localStorage.getItem('_bwm_attribution') || '{}'); } catch (_) {}
    const params = new URLSearchParams(window.location.search);
    const cookie = (name) => {
      const match = document.cookie.match(new RegExp('(?:^|; )' + name.replace(/[.$?*|{}()[\]\\/+^]/g, '\\$&') + '=([^;]*)'));
      return match ? decodeURIComponent(match[1]) : '';
    };
    return {
      attribution: stored,
      referrer: sessionStorage.getItem('bwm_referrer') || document.referrer || '',
      landing_page: sessionStorage.getItem('bwm_landing_page') || window.location.href,
      utm_source: params.get('utm_source') || '',
      utm_medium: params.get('utm_medium') || '',
      utm_campaign: params.get('utm_campaign') || '',
      utm_content: params.get('utm_content') || '',
      utm_term: params.get('utm_term') || '',
      fbclid: params.get('fbclid') || '',
      gclid: params.get('gclid') || '',
      fbc: cookie('_fbc'),
      fbp: cookie('_fbp'),
      booking_event_id: typeof window.__bwmEnsureBookingEventId === 'function' ? window.__bwmEnsureBookingEventId() : '',
    };
  }

  // MVT (PROJ-AUTO-MVT-001): the homepage hero experiment converts here on /book.
  // window.__BWM_EXP is injected by the edge worker on pages it serves (/, /go/*);
  // /book is not worker-served, so the variant arrives via the _bwm_exp_host carry
  // cookie set on the homepage. variant_id -> experiment_variant_id (the handler's
  // field name; variant_id already means utm_content elsewhere). Empty when the
  // visitor was never assigned (direct-to-/book), which the handler treats as no-op.
  function experimentContext() {
    const g = window.__BWM_EXP;
    if (g && g.experiment_id && g.variant_id) {
      return { experiment_id: g.experiment_id, experiment_variant_id: g.variant_id };
    }
    const m = document.cookie.match(/(?:^|; )_bwm_exp_host=([^;]*)/);
    if (m) {
      try {
        const c = JSON.parse(decodeURIComponent(m[1]));
        if (c && c.e && c.v) return { experiment_id: c.e, experiment_variant_id: c.v };
      } catch (_) {}
    }
    return { experiment_id: '', experiment_variant_id: '' };
  }

  function payloadFromForm() {
    const fd = new FormData(form);
    return Object.assign({
      first_name: fd.get('first_name'),
      email: fd.get('email'),
      company: fd.get('company'),
      trade: fd.get('trade'),
      service_area: fd.get('service_area'),
      selected_poor_four: fd.getAll('selected_poor_four'),
      lead_flow_state: fd.get('lead_flow_state'),
      follow_up_state: fd.get('follow_up_state'),
      agency_history: fd.get('agency_history'),
      ai_comfort: fd.get('ai_comfort'),
      ad_spend_readiness: fd.get('ad_spend_readiness'),
      revenue_range: fd.get('revenue_range'),
      offer_interest: fd.get('offer_interest'),
      decision_maker: fd.get('decision_maker'),
    }, attributionPayload(), experimentContext());
  }

  function labelForValue(name, value) {
    const field = form.querySelector(`[name="${name}"][value="${value}"]`);
    const label = field?.closest('label');
    return label ? label.textContent.trim().replace(/\s+/g, ' ') : String(value || 'Pending');
  }

  function poorFourLabel(value) {
    const field = form.querySelector(`[name="selected_poor_four"][value="${value}"]`);
    return field?.closest('label')?.querySelector('strong')?.textContent?.trim() || String(value || 'Pending');
  }

  function updateMapRail() {
    const fd = new FormData(form);
    const company = String(fd.get('company') || '').trim();
    const trade = String(fd.get('trade') || '').trim();
    const area = String(fd.get('service_area') || '').trim();
    const selectedLeaks = fd.getAll('selected_poor_four');
    const statusByStep = {
      1: 'Identity layer open',
      2: 'Leak signature forming',
      3: 'Revenue path mapped',
      4: 'Prior attempts logged',
      5: 'Routing decision ready',
    };

    if (railStatus) railStatus.textContent = statusByStep[currentStep] || 'Map building';
    if (railMeter) railMeter.style.transform = `scaleX(${currentStep / steps.length})`;
    if (railBusiness) {
      railBusiness.textContent = company || trade || area
        ? [company || 'Company pending', trade || 'Trade pending', area || 'Market pending'].join(' · ')
        : 'Waiting';
    }
    if (railLeaks) {
      railLeaks.textContent = selectedLeaks.length
        ? selectedLeaks.map(poorFourLabel).join(', ')
        : 'None selected yet';
    }
    if (railRevenue) {
      const lead = fd.get('lead_flow_state');
      const follow = fd.get('follow_up_state');
      railRevenue.textContent = lead || follow
        ? `${lead ? labelForValue('lead_flow_state', lead) : 'Lead flow pending'} · ${follow ? labelForValue('follow_up_state', follow) : 'Follow-up pending'}`
        : 'Pending';
    }
    if (railReadiness) {
      const ad = fd.get('ad_spend_readiness');
      const decision = fd.get('decision_maker');
      railReadiness.textContent = ad || decision
        ? `${ad ? labelForValue('ad_spend_readiness', ad) : 'Spend readiness pending'} · ${decision ? labelForValue('decision_maker', decision) : 'Decision role pending'}`
        : 'Pending';
    }
  }

  function revealLeakMap(payload) {
    const map = document.getElementById('market-leak-map');
    if (!map) return;
    const leakLabels = payload.selected_poor_four.length
      ? payload.selected_poor_four.map(poorFourLabel).join(', ')
      : 'No primary leak selected';
    map.querySelector('[data-map-leaks]').textContent = leakLabels;
    map.querySelector('[data-map-revenue]').textContent = labelForValue('lead_flow_state', payload.lead_flow_state);
    map.querySelector('[data-map-readiness]').textContent = labelForValue('ad_spend_readiness', payload.ad_spend_readiness);
    map.hidden = false;
  }

  nextBtn.addEventListener('click', () => {
    if (!validateStep()) return;
    if (currentStep === 1) event('diagnostic_start', { form_id: 'fit-diagnostic' });
    showStep(currentStep + 1);
  });
  mobileNextBtn?.addEventListener('click', () => {
    if (!validateStep()) return;
    event('diagnostic_start', { form_id: 'fit-diagnostic', entry: 'mobile_header' });
    showStep(currentStep + 1);
  });

  prevBtn.addEventListener('click', () => showStep(currentStep - 1));
  form.addEventListener('input', updateMapRail);
  form.addEventListener('change', updateMapRail);

  form.addEventListener('submit', async (e) => {
    e.preventDefault();
    if (!validateStep()) return;
    errorEl.hidden = true;
    submitBtn.disabled = true;
    submitBtn.querySelector('.q-submit-label').hidden = true;
    submitBtn.querySelector('.q-submit-loading').hidden = false;

    const payload = payloadFromForm();

    try {
      const res = await fetch('https://bwm-form-handler.robert-ba0.workers.dev/buildwisemedia/discovery-qualifier', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload),
      });

      if (!res.ok) throw new Error(`Server ${res.status}`);
      const data = await res.json();

      event('diagnostic_submit', {
        form_id: 'fit-diagnostic',
        classification: data.classification,
        selected_poor_four: payload.selected_poor_four.join(','),
        offer_interest: payload.offer_interest,
        contact_id: data.contact_id || '',
        capi_event_id: data.capi_event_id || '',
        booking_event_id: payload.booking_event_id || '',
      });

      if (data.classification === 'qualified') {
        const bookingEventId = typeof window.__bwmEnsureBookingEventId === 'function'
          ? window.__bwmEnsureBookingEventId(payload.booking_event_id)
          : payload.booking_event_id;
        const prefill = Object.assign({}, attributionPayload(), {
          name: payload.first_name,
          email: payload.email,
          tier_routing: data.tier_routing,
          contact_id: data.contact_id || '',
          capi_event_id: data.capi_event_id || '',
          booking_event_id: bookingEventId || payload.booking_event_id || '',
        });

        event('generate_lead', {
          currency: 'USD',
          value: 0,
          form_id: 'fit-diagnostic',
          tier_routing: data.tier_routing,
          classification: data.classification,
          selected_poor_four: payload.selected_poor_four.join(','),
          offer_interest: payload.offer_interest,
          contact_id: data.contact_id || '',
          capi_event_id: data.capi_event_id || '',
          booking_event_id: prefill.booking_event_id,
        });

        revealLeakMap(payload);
        form.style.display = 'none';
        const calWrap = document.getElementById('cal-inline');
        if (calWrap) {
          calWrap.removeAttribute('hidden');
            calWrap.scrollIntoView({ behavior: 'smooth', block: 'start' });
        }
        window.__qualifiedPrefill = prefill;
        if (window.__loadBwmCalendar) {
          window.__loadBwmCalendar(prefill);
        } else if (window.Cal && window.Cal.ns && window.Cal.ns['discovery-call']) {
          window.Cal.ns['discovery-call']('inline', {
            elementOrSelector: '#cal-inline',
            calLink: 'bwm/discovery-call',
            config: {
              name: payload.first_name,
              email: payload.email,
              theme: 'dark',
              metadata: prefill,
            },
          });
        }
      } else if (data.classification === 'pending') {
        // Manual-review route: the lead IS captured (the team replies by email).
        // Previously fell into the error branch and told a captured lead
        // "something went wrong" (found 2026-06-11).
        form.style.display = 'none';
        const note = document.createElement('p');
        note.className = 'q-pending-note';
        note.textContent = 'Thanks — your details are in. The team reviews every diagnostic by hand and will email your Revenue Leak Map and next steps within one business day.';
        form.insertAdjacentElement('afterend', note);
      } else if (data.classification === 'disqualified') {
        const reason = encodeURIComponent(data.reason || 'fit_check');
        // brief deferral lets gtag flush the diagnostic_submit beacon before
        // the page unloads — instant redirect aborted the hit (2026-06-11).
        setTimeout(() => { window.location.href = `/thank-you-resource?reason=${reason}`; }, 250);
      } else {
        throw new Error('Unexpected response');
      }
    } catch (err) {
      console.error('Diagnostic submit failed:', err);
      errorEl.textContent = 'Something went wrong. Please try again, or email hello@buildwisemedia.com.';
      errorEl.hidden = false;
      submitBtn.disabled = false;
      submitBtn.querySelector('.q-submit-label').hidden = false;
      submitBtn.querySelector('.q-submit-loading').hidden = true;
    }
  });

  showStep(1);
})();
</script>

<style>
.qualifier-form,
.market-leak-map {
  width: 100%;
  max-width: 100%;
  display: flex;
  flex-direction: column;
  gap: 20px;
  background:
    linear-gradient(135deg, rgba(56, 216, 255, 0.055), transparent 42%),
    linear-gradient(225deg, rgba(240, 255, 0, 0.04), transparent 46%),
    var(--bg-elev);
  border: 1px solid var(--rule);
  padding: clamp(22px, 3vw, 32px);
}
.qualifier-form [hidden] { display: none !important; }
.market-leak-map[hidden] { display: none; }
.q-progress {
  height: 3px;
  background: var(--rule);
  overflow: hidden;
}
.q-progress-bar {
  display: block;
  width: 100%;
  height: 100%;
  background: var(--y2);
  transform: scaleX(0.2);
  transform-origin: left;
  transition: transform 180ms ease;
}
.q-head {
  display: flex;
  flex-direction: column;
  gap: 10px;
  max-width: 760px;
}
.q-eyebrow,
.q-step-kicker {
  font-size: 10px;
  letter-spacing: 0.2em;
  color: var(--y2);
  margin: 0;
}
.q-heading {
  font-family: var(--sans);
  font-weight: 800;
  font-size: clamp(22px, 2.5vw, 32px);
  line-height: 1.1;
  letter-spacing: -0.03em;
  margin: 0;
  color: var(--ink);
  overflow-wrap: anywhere;
  text-wrap: balance;
}
.q-sub {
  font-size: 14px;
  line-height: 1.5;
  color: var(--ink-dim);
  margin: 0;
  max-width: 68ch;
}
.q-step {
  display: none;
  padding: 0;
}
.q-step.is-active {
  display: flex;
  flex-direction: column;
  gap: 18px;
}
.q-layout {
  display: grid;
  grid-template-columns: minmax(0, 1fr) minmax(220px, 0.36fr);
  gap: clamp(18px, 2.5vw, 24px);
  align-items: start;
}
.q-layout > * { min-width: 0; }
.q-workspace {
  min-width: 0;
  display: flex;
  flex-direction: column;
  gap: 18px;
}
.q-step-title {
  font-size: clamp(24px, 2.6vw, 36px);
  line-height: 1.1;
  margin: 0;
}
.q-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 12px;
}
.q-grid > * { min-width: 0; }
.q-row,
.q-fieldset {
  display: flex;
  flex-direction: column;
  gap: 8px;
}
.q-row--wide { grid-column: 1 / -1; }
.q-label {
  font-family: var(--mono);
  font-size: 10px;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--ink-dim);
}
.q-row input,
.q-row select {
  width: 100%;
  min-width: 0;
  min-height: 48px;
  background: rgba(5, 5, 7, 0.68);
  border: 1px solid var(--rule);
  color: var(--ink);
  padding: 13px 14px;
  font-family: var(--sans);
  font-size: 15px;
  outline: none;
}
.q-row input:focus,
.q-row select:focus {
  border-color: var(--y2);
}
.q-fieldset {
  border: none;
  padding: 0;
  margin: 0;
}
.q-fieldset legend {
  padding: 0;
  margin-bottom: 4px;
}
.q-options {
  display: flex;
  flex-direction: column;
  gap: 8px;
}
.q-options--grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 12px;
}
.q-options label,
.q-option-card {
  display: flex;
  align-items: flex-start;
  gap: 12px;
  min-width: 0;
  min-height: 48px;
  padding: 12px 13px;
  border: 1px solid var(--rule);
  background: rgba(5, 5, 7, 0.42);
  cursor: pointer;
  font-size: 14px;
  color: var(--ink-dim);
  transition: border-color 150ms ease, color 150ms ease, background 150ms ease;
}
.q-options label span,
.q-option-card span {
  min-width: 0;
}
.q-options label:hover,
.q-option-card:hover {
  border-color: var(--ink-faint);
}
.q-options input[type="radio"],
.q-options input[type="checkbox"] {
  appearance: none;
  width: 15px;
  height: 15px;
  border: 1px solid var(--ink-faint);
  border-radius: 50%;
  margin: 3px 0 0;
  flex-shrink: 0;
  position: relative;
}
.q-options input[type="checkbox"] {
  border-radius: 2px;
}
.q-options input:checked {
  border-color: var(--y2);
}
.q-options input:checked::after {
  content: '';
  position: absolute;
  inset: 3px;
  background: var(--y2);
  border-radius: inherit;
}
.q-options label:has(input:checked),
.q-option-card:has(input:checked) {
  border-color: var(--signal-cyan);
  color: var(--ink);
  background: rgba(56, 216, 255, 0.055);
}
.q-option-card strong {
  display: block;
  color: var(--ink);
  margin-bottom: 5px;
}
.q-option-card em {
  display: block;
  font-style: normal;
  font-size: 13px;
  line-height: 1.45;
  color: var(--ink-dim);
}
.q-actions {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
  align-items: center;
  padding-top: 2px;
}
.q-submit,
.q-secondary {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  padding: 16px 24px;
  font-family: var(--sans);
  font-weight: 700;
  font-size: 15px;
  cursor: pointer;
}
.q-submit {
  background: var(--y2);
  color: var(--bg);
  border: 1px solid var(--y2);
  min-width: min(100%, 260px);
  margin-left: auto;
}
.q-secondary {
  border: 1px solid var(--rule-bright);
  color: var(--ink-dim);
  min-width: 104px;
  background: transparent;
}
.q-submit:hover:not(:disabled),
.q-secondary:hover {
  opacity: 0.85;
}
.q-submit:disabled {
  cursor: wait;
  opacity: 0.6;
}
.q-mobile-start {
  display: none;
  align-self: flex-start;
  margin-top: 4px;
  min-width: min(100%, 220px);
  margin-left: 0;
}
.q-error {
  color: #ff6b6b;
  font-size: 14px;
  margin: 0;
}
.q-map-rail {
  position: sticky;
  top: 96px;
  border: 1px solid var(--rule-bright);
  background:
    linear-gradient(180deg, rgba(56, 216, 255, 0.05), transparent 36%),
    rgba(5, 5, 7, 0.72);
  padding: 16px;
  display: flex;
  flex-direction: column;
  gap: 14px;
}
.q-map-top {
  display: flex;
  flex-direction: column;
  gap: 8px;
}
.q-map-top strong {
  font-size: clamp(22px, 2vw, 30px);
  line-height: 1;
  letter-spacing: -0.035em;
  color: var(--ink);
}
.q-map-meter {
  height: 4px;
  background: var(--rule);
  overflow: hidden;
}
.q-map-meter span {
  display: block;
  width: 100%;
  height: 100%;
  transform: scaleX(0.2);
  transform-origin: left;
  background: linear-gradient(90deg, var(--signal-cyan), var(--y2), var(--signal-green));
  transition: transform 180ms ease;
}
.q-map-signals {
  display: grid;
  gap: 1px;
  background: var(--rule);
  border: 1px solid var(--rule);
}
.q-map-signals div {
  background: var(--bg);
  padding: 13px;
}
.q-map-signals dt {
  color: var(--ink-dim);
  font-size: 9px;
  letter-spacing: 0.16em;
  text-transform: uppercase;
}
.q-map-signals dd {
  margin-top: 7px;
  color: var(--ink);
  font-size: 13px;
  line-height: 1.35;
}
.q-map-routing {
  display: grid;
  grid-template-columns: 12px minmax(0, 1fr);
  gap: 10px;
  align-items: start;
  color: var(--ink-dim);
  font-size: 12px;
  line-height: 1.45;
}
.q-map-routing .dot { margin-top: 6px; }
.map-grid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 1px;
  background: var(--rule);
  border: 1px solid var(--rule);
}
.map-grid > div {
  background: var(--bg);
  padding: 18px;
}
.map-label {
  font-size: 9px;
  letter-spacing: 0.18em;
  color: var(--ink-faint);
}
.map-value {
  margin-top: 10px;
  font-size: 18px;
  line-height: 1.25;
  color: var(--ink);
}
.map-note {
  color: var(--ink-dim);
  font-size: 15px;
  line-height: 1.55;
}

@media (max-width: 680px) {
  .qualifier-form,
  .market-leak-map {
    gap: 18px;
    padding: 24px;
  }

  .q-heading {
    font-size: 30px;
    line-height: 1.02;
  }

  .q-sub {
    font-size: 15px;
    line-height: 1.45;
  }

  .q-mobile-start:not([hidden]) {
    display: inline-flex;
    width: 100%;
    min-height: 58px;
    margin-top: 6px;
  }

  .q-grid,
  .q-options--grid,
  .map-grid {
    grid-template-columns: 1fr;
  }
  .q-layout { grid-template-columns: 1fr; }
  .q-map-rail {
    display: none;
  }
  .q-map-signals dd {
    overflow-wrap: anywhere;
  }
  .q-submit,
  .q-secondary {
    width: 100%;
    min-width: 0;
    margin-left: 0;
  }
  .q-actions { gap: 10px; }
  .q-secondary {
    order: 2;
    padding: 12px 18px;
  }
}
</style>
