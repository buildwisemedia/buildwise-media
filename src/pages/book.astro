---
import BaseLayout from '../layouts/BaseLayout.astro';
import QualifierForm from '../components/QualifierForm.astro';

const title = "Book a Discovery Call — Buildwise Media";
const description = "Build your Market Leak Map, then book a discovery call if Ascend looks like a fit.";

const breadcrumb = {
  '@context': 'https://schema.org',
  '@type': 'BreadcrumbList',
  itemListElement: [
    { '@type': 'ListItem', position: 1, name: 'Home', item: 'https://buildwisemedia.com/' },
    { '@type': 'ListItem', position: 2, name: 'Book', item: 'https://buildwisemedia.com/book' },
  ],
};
---
<BaseLayout title={title} description={description} jsonLd={[breadcrumb]}>

  <section class="book-page">
    <div class="container book-grid">

      <aside class="book-context">
        <p class="eyebrow">Fit diagnostic</p>
        <h1 class="h-hero">
          Build the leak map.<br/>
          Then book the call.
        </h1>
        <p class="hero-sub">
          The diagnostic names the active Poor Four gaps, captures the context, and routes qualified owners into a short discovery call.
        </p>

        <ul class="expect-list">
          <li>
            <p class="expect-label mono">What we'll cover</p>
            <p class="expect-body">Which market leaks are active, where the system would intervene, and what the install timeline looks like for your current operating layer.</p>
          </li>
          <li>
            <p class="expect-label mono">How long</p>
            <p class="expect-body">The diagnostic takes about five minutes. The call is short because the map does the setup work first.</p>
          </li>
          <li>
            <p class="expect-label mono">No obligation</p>
            <p class="expect-body">Whether we engage or not, you leave with the failure-mode framing. Use it with us, with another operator, or internally.</p>
          </li>
        </ul>
      </aside>

      <div class="book-embed">
        <QualifierForm />

        <div id="cal-inline" class="cal-frame" aria-label="Booking calendar" hidden>
          <div class="cal-skeleton" aria-hidden="true">
            <div class="sk-row"></div>
            <div class="sk-row"></div>
            <div class="sk-grid">
              <span></span><span></span><span></span><span></span><span></span><span></span><span></span>
              <span></span><span></span><span></span><span></span><span></span><span></span><span></span>
              <span></span><span></span><span></span><span></span><span></span><span></span><span></span>
            </div>
            <p class="sk-foot mono">Loading calendar…</p>
          </div>
          <a id="book-cta-fallback" class="book-cta" href="https://book.buildwisemedia.com/bwm/discovery-call" rel="noopener" target="_blank">
            Open booking calendar <span aria-hidden="true">→</span>
          </a>
        </div>
      </div>

    </div>
  </section>

</BaseLayout>

<script is:inline>
  (function () {
    const origin = "https://book.buildwisemedia.com";
    const embedSrc = `${origin}/embed/embed.js`;
    const calBrandTokens = {
      "cal-brand": "#F0FF00",
      "cal-brand-emphasis": "#050507",
      "cal-brand-foreground": "#050507",
      "cal-text-brand": "#050507",
      "cal-bg-brand": "#F0FF00",
      "cal-brand-text": "#050507",
    };

    function installCalStub(C, A, L) {
      if (C.Cal && C.Cal.loaded) return;
      const p = function (a, ar) { a.q.push(ar); };
      const d = C.document;
      C.Cal = C.Cal || function () {
        const cal = C.Cal;
        const ar = arguments;
        if (!cal.loaded) {
          cal.ns = {};
          cal.q = cal.q || [];
          d.head.appendChild(d.createElement("script")).src = A;
          cal.loaded = true;
        }
        if (ar[0] === L) {
          const api = function () { p(api, arguments); };
          const namespace = ar[1];
          api.q = api.q || [];
          if (typeof namespace === "string") {
            cal.ns[namespace] = cal.ns[namespace] || api;
            p(cal.ns[namespace], ar);
            p(cal, ["initNamespace", namespace]);
          } else {
            p(cal, ar);
          }
          return;
        }
        p(cal, ar);
      };
    }

    window.__loadBwmCalendar = function loadBwmCalendar(prefill) {
      const target = document.getElementById('cal-inline');
      if (!target) return;
      target.removeAttribute('hidden');
      installCalStub(window, embedSrc, "init");
      Cal("init", "discovery-call", { origin });
      Cal.ns["discovery-call"]("inline", {
        elementOrSelector: "#cal-inline",
        config: Object.assign({ layout: "month_view", theme: "dark" }, prefill || {}),
        calLink: "bwm/discovery-call",
      });
      Cal.ns["discovery-call"]("ui", {
        theme: "dark",
        styles: { branding: { brandColor: "#F0FF00" } },
        cssVarsPerTheme: { light: calBrandTokens, dark: calBrandTokens },
        hideEventTypeDetails: false,
        layout: "month_view",
      });
    };
  })();

  // Hide skeleton + fallback CTA when iframe paints
  const obs = new MutationObserver(() => {
    const el = document.getElementById('cal-inline');
    if (el && el.querySelector('iframe')) {
      const sk = el.querySelector('.cal-skeleton');
      if (sk) sk.remove();
      const cta = document.getElementById('book-cta-fallback');
      if (cta) cta.remove();
      obs.disconnect();
    }
  });
  obs.observe(document.getElementById('cal-inline'), { childList: true, subtree: true });

  // Paid-traffic fast-path: visitors already saw the ad copy upstream, so skip
  // the explainer aside and scroll straight to the diagnostic on page load.
  // Keeps organic / direct traffic on the full-explainer experience.
  (function () {
    try {
      const params = new URLSearchParams(window.location.search);
      const utmSource = (params.get('utm_source') || '').toLowerCase();
      if (utmSource === 'facebook' || utmSource === 'instagram' || utmSource === 'meta') {
        // Defer one tick so layout settles before scroll
        requestAnimationFrame(function () {
          const target = document.getElementById('qualifier-form') || document.getElementById('cal-inline');
          if (target) target.scrollIntoView({ behavior: 'instant', block: 'start' });
        });
      }
    } catch (_e) { /* no-op */ }
  })();
</script>

<style>
.book-page {
  padding-top: clamp(96px, 10vw, 180px);
  padding-bottom: var(--section-y);
}

/* Scope hero size down on /book — the global .h-hero is sized for the
   homepage where it's the centerpiece. On /book the calendar is the
   centerpiece, so the headline shares space. Earlier attempt at
   clamp(36-64px) still let "Fifteen minutes." wrap on a ~458px left
   column. Tightened to 28-48px + !important to win against any cached
   global rule, and balance="balance" on the h1 to break naturally. */
.book-page .h-hero {
  font-size: clamp(28px, 3.2vw, 48px) !important;
  line-height: 1 !important;
  text-wrap: balance;
}
.book-grid {
  display: grid;
  grid-template-columns: minmax(0, 1fr) minmax(0, 1.4fr);
  gap: clamp(48px, 6vw, 80px);
  align-items: start;
}
.book-context { display: flex; flex-direction: column; gap: 24px; position: sticky; top: 100px; }

.expect-list { list-style: none; display: flex; flex-direction: column; gap: 24px; padding-top: 32px; border-top: 1px solid var(--rule); margin-top: 16px; }
.expect-list li { display: flex; flex-direction: column; gap: 8px; }
.expect-label { font-size: 10px; letter-spacing: 0.2em; color: var(--y2); }
.expect-body { font-size: 15px; font-weight: 300; line-height: 1.6; color: var(--ink-dim); }

.book-embed { width: 100%; min-height: 720px; display: flex; flex-direction: column; gap: 16px; }

/* Fallback CTA — visible until iframe paints, then JS removes. Gives mobile users
   a tappable path even when the booking embed is still loading. Per EXP-002 telemetry:
   50% of ad clickers were dropping before LPV; primary suspect = embed cold-start. */
.book-cta {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 12px;
  padding: 18px 28px;
  background: var(--y2, #F0FF00);
  color: #050507;
  text-decoration: none;
  font-family: 'Inter', sans-serif;
  font-weight: 700;
  font-size: 16px;
  letter-spacing: -0.01em;
  border: 1px solid var(--y2, #F0FF00);
  align-self: stretch;
  text-align: center;
  transition: opacity 0.15s ease;
}
.book-cta:hover, .book-cta:focus-visible { opacity: 0.85; }
.book-cta span { font-weight: 400; }

.cal-frame {
  width: 100%;
  min-height: 720px;
  background: var(--bg-elev);
  border: 1px solid var(--rule);
  position: relative;
  overflow: hidden;
}
.cal-frame iframe { background: var(--bg-elev) !important; }

.cal-skeleton { padding: 32px; display: flex; flex-direction: column; gap: 16px; height: 100%; }
.sk-row { height: 16px; background: linear-gradient(90deg, var(--rule), var(--rule-bright), var(--rule)); background-size: 200% 100%; animation: sk 1.4s ease-in-out infinite; border-radius: 1px; }
.sk-row:nth-child(1) { width: 70%; }
.sk-row:nth-child(2) { width: 40%; margin-bottom: 16px; }
.sk-grid { display: grid; grid-template-columns: repeat(7, 1fr); gap: 8px; flex: 1; }
.sk-grid span { background: linear-gradient(90deg, var(--rule), var(--rule-bright), var(--rule)); background-size: 200% 100%; animation: sk 1.4s ease-in-out infinite; border-radius: 1px; }
.sk-foot { margin-top: 16px; color: var(--ink-faint); font-size: 10px; letter-spacing: 0.2em; text-align: center; }
@keyframes sk { 0% { background-position: 0% 0; } 100% { background-position: -200% 0; } }
@media (prefers-reduced-motion: reduce) { .sk-row, .sk-grid span { animation: none; } }

@media (max-width: 880px) {
  .book-grid {
    grid-template-columns: 1fr;
    /* Mobile: surface the calendar / CTA above the explainer. Paid-traffic JS
       also auto-scrolls to the embed; this is the no-JS / organic-mobile fallback. */
    grid-template-areas: "embed" "context";
  }
  .book-context { position: static; grid-area: context; }
  .book-embed { grid-area: embed; min-height: auto; }
  .cal-frame { min-height: 560px; }
  .book-cta { padding: 16px 24px; font-size: 15px; }
}
</style>
